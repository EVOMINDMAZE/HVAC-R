1. **Document Workaround:** Create `SUPABASE_CLI_GUIDE.md` with the working commands for restricted networks.
2. **Push Migration:** Run `npx supabase db push` using the transaction port (6543) and HTTPS DNS flags to apply the backend fixes to the cloud database.
3. **Run Final Tests:** Execute `npx tsx scripts/test_browser_scenarios.ts` and provide the final PASS/FAIL report for all 9 scenarios.

